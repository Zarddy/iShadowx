<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="LoadingDialog">
        <!--对话框背景-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--边框-->
        <item name="android:windowFrame">@null</item>
        <!--没有标题-->
        <item name="android:windowNoTitle">true</item>
        <!--是否浮在Activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--背景透明-->
        <item name="android:windowIsTranslucent">false</item>
        <!--是否有覆盖-->
        <item name="android:windowContentOverlay">@null</item>
        <!--背景变暗-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="line_h">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1px</item>
        <item name="android:background">@color/stroke</item>
    </style>

    <style name="line_h_1dp" parent="@style/line_h">
        <item name="android:layout_height">1dp</item>
    </style>

    <style name="line_v">
        <item name="android:layout_width">1px</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/stroke</item>
    </style>

    <style name="line_v_1dp" parent="@style/line_v">
        <item name="android:layout_width">1dp</item>
    </style>

    <!-- 普通弹出方式的对话框主题 -->
    <style name="dialog_normal" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>

        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>

        <item name="android:padding">0dp</item>
        <item name="android:layout_margin">0dp</item>
    </style>

    <!-- 从下方弹出的对话框主题弹出动画 -->
    <style name="dialog_push_up_anim" parent="@android:style/Animation.Dialog">

        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>

        <item name="android:padding">0dp</item>
        <item name="android:layout_margin">0dp</item>

        <item name="android:windowEnterAnimation">@anim/push_up_in</item>
        <item name="android:windowExitAnimation">@anim/pull_down_out</item>
    </style>

    <!-- LoadingDialog 普通弹出方式的对话框主题 -->
    <style name="loading_dialog_normal" parent="dialog_normal">
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>